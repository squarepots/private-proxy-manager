# Gemini CLI entry

@./AGENTS.md

Private Proxy Manager's canonical portable Agent Skill is `.agents/skills/private-proxy-manager/SKILL.md`. Gemini CLI recognizes `.agents/skills/` as a workspace-skill alias; use that Skill instead of duplicating its instructions here.

Use the same `agent/ppm-agent.ps1` machine surface and product/safety semantics as other capable agent runtimes.
