# Claude Code entry

Read `AGENTS.md` first. For Private Proxy Manager operation, activate/read the canonical portable Skill at `.agents/skills/private-proxy-manager/SKILL.md` and follow it as the product contract.

Do not create Claude-specific copies of the domain model, preflight rules, or operation semantics. Use the same `agent/ppm-agent.ps1` machine surface as other capable agent runtimes.
