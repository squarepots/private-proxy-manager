interface:
  display_name: "Route Steward"
  short_description: "Use AI to manage networking on your own servers"
  default_prompt: "Use $route-steward to deploy, check, migrate, and recover networking on servers I manage. Read the repository contract, inspect capabilities, run quick validation, explain host-wide effects, keep sensitive state private, and ask me only for decisions or access you cannot safely infer."
