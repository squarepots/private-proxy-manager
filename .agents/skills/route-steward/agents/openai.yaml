interface:
  display_name: "Route Steward"
  short_description: "Let an AI operate your self-hosted network paths"
  default_prompt: "Use $route-steward to learn this repository, gather the context needed for my goal, and operate the project safely. Ask me only for decisions or access you cannot safely infer."
